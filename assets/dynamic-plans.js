(()=>{
'use strict';
const read=(k,d)=>{try{return JSON.parse(localStorage.getItem(k))??d}catch{return d}},write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
async function init(){if(location.pathname!=='/plans')return;try{const [plans,platform]=await Promise.all([fetch('/api/learning-plans',{headers:{Accept:'application/json'}}).then(r=>r.json()),fetch('/api/platform',{headers:{Accept:'application/json'}}).then(r=>r.json()).catch(()=>({}))]);const grid=document.querySelector('.product-grid');if(!grid)return;const symbol=platform.currency_symbol||'Rs';grid.innerHTML=plans.map((p,i)=>`<article class="product-card ${i===1?'featured':''}">${i===1?'<span class="suite-chip">Best value</span>':''}<h2>${esc(p.name)}</h2><p>${esc(p.description||'Flexible academy access.')}</p><div class="product-price">${esc(symbol)} ${Number(p.price||0).toLocaleString('en-PK')}<small>${p.billing_period==='monthly'?'/month':p.billing_period==='annual'?'/year':p.billing_period==='lifetime'?' lifetime':''}</small></div><ul class="feature-list">${(p.features||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul><button class="suite-primary" data-dynamic-plan="${esc(p.slug)}">Start ${esc(p.name)} plan</button></article>`).join('')+`<article class="product-card"><h2>Buy individual courses</h2><p>Prefer lifetime access to selected courses?</p><div class="product-price">Choose a course</div><ul class="feature-list"><li>Lifetime course access</li><li>Course certificate</li><li>Q&A and notes</li><li>Practice activities</li></ul><a class="suite-primary" href="/courses">Browse courses</a></article>`;grid.addEventListener('click',e=>{const b=e.target.closest('[data-dynamic-plan]');if(!b)return;write('bwa_subscription',{active:true,plan:b.dataset.dynamicPlan,started:new Date().toISOString()});const s=document.querySelector('#planStatus');if(s)s.textContent=`Active plan · ${b.dataset.dynamicPlan}`;if(typeof window.BWABackend!=='undefined')localStorage.setItem('bwa_subscription',JSON.stringify({active:true,plan:b.dataset.dynamicPlan,started:new Date().toISOString()}))});}catch(e){console.warn('[BWA plans]',e.message)}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
